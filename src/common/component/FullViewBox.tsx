import {
	styled,
} from '@mui/system';

// ------------------------------------------------

const FullViewBox = styled('div')({
	position: 'absolute',
	width: '100%',
	height: '100%',
	overflow: 'hidden',
});

// ------------------------------------------------

export { FullViewBox };